{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "start:dev": {
      "persistent": true,
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "build/**"]
    }
  }
}
